# Minimal .gitignore - only essential build artifacts
# Keeping secret files and configs as requested by user

# Flutter/Dart/Pub related - only essential build files
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Android Studio build artifacts
/android/app/debug
/android/app/profile
/android/app/release

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Note: Secret files like google-services.json, key.properties, etc. are intentionally NOT ignored
# as per user request to not hide any secret files
